%check if the given block is a bus selector block or demux block and inport
%does not connect with a inport block (or TL inport block)
% function re=mxam_BusSelectorDemuxNotAfterInport(hBlock)
%
% RETURN PARAMETERS:
% ==================
% re -- true if the given block is a bus selector block or demux block and
% inport does not connect with a inport block (or TL inport block)
%
% INPUT PARAMETERS:
% =================
% hBlock-- handle of the block
%

function re=mxam_BusSelectorDemuxNotAfterInport(hBlock)
































































