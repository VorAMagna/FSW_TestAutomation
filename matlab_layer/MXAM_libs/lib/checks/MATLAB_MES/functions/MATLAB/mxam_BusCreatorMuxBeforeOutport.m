%check if the given block is a bus creator block or mux block and connects
%with an outport( or tl_outport)
% function re=mxam_BusCreatorMuxBeforeOutport(hBlock)
%
% RETURN PARAMETERS:
% ==================
% re -- true if the given block is a a bus creator block or mux block and
% connects with an outport( or tl_outport)
%
% INPUT PARAMETERS:
% =================
% hBlock-- handle of the block
%

function re=mxam_BusCreatorMuxBeforeOutport(hBlock)




























































