%check if the given block is a bus selector block or demux block and inport
%connects with a inport block (or TL inport block)
% function re=mxam_BusSelectorDemuxAfterInport(hBlock)
%
% RETURN PARAMETERS:
% ==================
% re -- true if the given block is a bus selector block or demux block and
% inport connects with a inport block (or TL inport block)
%
% INPUT PARAMETERS:
% =================
% hBlock-- handle of the block
%

function re=mxam_BusSelectorDemuxAfterInport(hBlock)




























































