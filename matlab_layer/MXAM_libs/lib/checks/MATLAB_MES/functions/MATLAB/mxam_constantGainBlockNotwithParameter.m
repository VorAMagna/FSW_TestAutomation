%check if the given block is a constant(or tl_constant) or gain(or tl_gain)
%block not with a parameter in constat value or gain field
% function re=mxam_constantGainBlockNotwithParameter(hBlock)
%
% RETURN PARAMETERS:
% ==================
% re -- true if the given block is a constant(or tl_constant) or gain(or
% tl_gain) block not with a parameter in constat value or gain field
%
% INPUT PARAMETERS:
% =================
% hBlock-- handle of the block
%

function re=mxam_constantGainBlockNotwithParameter(hBlock)


































































