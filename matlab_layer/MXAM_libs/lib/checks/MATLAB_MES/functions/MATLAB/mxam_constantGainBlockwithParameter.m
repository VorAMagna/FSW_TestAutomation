%check if the given block is a constant(or tl_constant) or gain(or tl_gain)
%block with a parameter(not a constant value) in constat value or gain field
% function re=mxam_constantGainBlockwithParameter(hBlock)
%
% RETURN PARAMETERS:
% ==================
% re -- true if the given block is a constant(or tl_constant) or gain(or
% tl_gain) block with a parameter(not a constant value) in constat value or
% gain field
%
% INPUT PARAMETERS:
% =================
% hBlock-- handle of the block
%

function re=mxam_constantGainBlockwithParameter(hBlock)
































































