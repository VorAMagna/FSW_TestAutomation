% Parses command string for occurence of s_key and gets associated value.
%
% SYNTAX:
% =======
% [s_val,s_keyout,s_cmdout,s_err,s_warn] =
% mes_parsecmdstr(s_key,s_cmd,cs_keyset)
%
% DESCRIPTION:
% ============
% Searches string for first occurence of "/<s_key>:" or
% "/<s_key>:<s_val>" and returns s_val, which is the string following the 
% colon until the first occurence of a space or a subsequent slash
% if s_val contains slashes or spaces (e.g. a path),
% it must be enclosed in delimiter chars s_delim (see below, 
% e.g. exclamation marks: s_cmd = '/path:!c:/data/my modell.mdl!/fix:user'

function [s_val,s_keyout,s_cmdout,s_err,s_warn] = mes_parsecmdstr(s_key,s_cmd,cs_keyset,b_ignorecase)






































































































































































































































