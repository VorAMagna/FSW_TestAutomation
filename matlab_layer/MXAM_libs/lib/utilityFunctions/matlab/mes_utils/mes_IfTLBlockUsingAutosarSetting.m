%Test if a block has autosar.* properties, if so, then it is a block with
%autosar tab (a inport or outport or busport generated in tl3.2).
%if the autosar tab is used then return true, otherwise, return false

function b_useAutosarSetting=mes_IfTLBlockUsingAutosarSetting(hBlock)






































































































